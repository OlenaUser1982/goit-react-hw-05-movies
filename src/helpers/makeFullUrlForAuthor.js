// export const makeFullUrlForAuthor = array => {
//   console.log(array);
//   return array
//     .map(({ author, id, content }) => {
//       return `${author}, ${id}, ${content}`;
//     })
//     .join(', ');
// };
